<cu-custom bgColor="bg-gradual-pink" isBack="{{true}}">
    <view slot="backText">返回</view>
    <view slot="content">预约安装</view>
</cu-custom>
<scroll-view>
    <view class="bg-white padding">
        <view class="cu-steps">
            <view class="cu-item {{index>currstep?'':'text-pink'}}" wx:for="{{steps}}" wx:key>
                <text class="cuIcon-{{item.icon}}"></text>
                {{item.name}}
            </view>
        </view>
    </view>
    <view class='margin-top'>
        <view class="btn-area" wx:if="{{null==addressInfo}}">
            <button type="primary" bindtap="chooseAddress">选择地址安装吧</button>
        </view>
        <view class='cu-card' wx:else>
            <view class="cu-bar bg-white solid-bottom">
                <view class="action">
                    <text class="cuIcon-title text-orange"></text>
                    预约安装地址
                </view>
                <view class="action" wx:if="{{currstep<2}}">
                    <button class="cu-btn bg-pink shadow" bindtap="changeAddress">更换</button>
                </view>
            </view>
            <view class='content bg-white padding text-cyan'>
                <view>
                    <text class="text-bold">{{addressInfo.userName}}</text>
                    ,{{addressInfo.telNumber}}
                </view>
                <view class='content'>
                    {{addressInfo.provinceName}} {{addressInfo.cityName}} {{addressInfo.countyName}} {{addressInfo.detailInfo}}
                </view>
            </view>
        </view>
    </view>
    <view class='btn-area margin-top' wx:if="{{currstep==3}}">
        <button type="primary" bindtap="pay">充值开始净水</button>
    </view>
    <view class="cu-timeline margin-top-sm">
        <view class="cu-item" wx:for="{{logs}}" wx:if="{{currstep>index}}">
            <view class='text-black'>{{item.content}}</view>
        </view>
    </view>
    <view class="cu-tabbar-height"></view>
</scroll-view>
<view class="flex cu-bar foot">
    <button class="cu-btn bg-pink shadow lg" bindtap="nextStep">模拟下一步</button>
</view>

<view class='cu-load load-modal' wx:if="{{loadModal}}">
  <view class='cuIcon-emojifill text-orange'></view>
  <!-- <image src='/images/logo.png' class='png' mode='aspectFit'></image> -->
  <view class='gray-text'>加载中...</view>
</view>